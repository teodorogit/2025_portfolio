'use client';
import Navbar from '../navbar';
import React from 'react';
import MainSection from '../MainSection/index';

const page = () => {
    return (
        <div>
            <Navbar />
            <MainSection />
        </div>
    );
};

export default page;
